# تسليم جلسة 6 - مشروع eboma / tradehub

## السياق العام
المسار: `C:\Users\Orange\Desktop\eboma`. React + tRPC + Drizzle ORM + MySQL/TiDB Cloud. هذي الجلسة (6) خصصت بالكامل لإكمال "تسوية الفريق" (كانت معلّقة من نهاية الجلسة 5) ثم بناء ميزة كبيرة جديدة: **دفع الحصص الفردية + رؤية الأسلاف لتفاصيل تابعيهم بأي عمق**.

## ✅ ما تم إنجازه هذي الجلسة (كل شي مختبر فعلياً بالمتصفح، مو بس بالكود)

### 1. إكمال "تسوية الفريق" (من الجلسة 5)
- سكربت `backfillProfitSettlementMerchants.ts` اشتغل بنجاح (1 inserted, 0 skipped) بعد إصلاح مشكلة `dotenv/config` غير محمّل عند التشغيل المباشر
- اختُبرت الميزة فعلياً بالمتصفح: معاينة، تأكيد تسوية جماعية (مشرف + مندوبين مباشرين محددين)، الأرقام صحيحة رياضياً
- **إصلاح إضافي اكتُشف أثناء الاختبار**: لو مندوب محدد بالـ checkboxes لكن ما عنده أي طلب مؤهل بالفترة/المنتج المحدد، كان ينضاف بصمت لجدول `profitSettlementMerchants` بدون أي مساهمة فعلية — تم تصحيحه: الآن يُفلتر تلقائياً (لا يُدرج إلا من ساهم بطلب واحد على الأقل ضمن `rows` المسحوبة)، مع تنبيه واضح بالواجهة (أصفر) يظهر فور اختيار الفترة/الأعضاء: "هذا التاجر لا يملك طلبات بهذه الفترة"

### 2. ميزة "دفع حصة واحدة لحالها" (جديدة بالكامل)
**المشكلة الأصلية**: تبويب "أرصدة التجار" كان يعرض رصيد متراكم فقط، وزر "تسوية الرصيد" يدفع كل الحصص المعلّقة دفعة وحدة — هذا يفقّد ربط كل حصة بسكرين إثبات الترويج الخاص فيها (خصوصاً لتجار يشتغلون بأكثر من منتج/تسوية).

**الحل المطبّق**:
- `settleSingleShare` (server/db.ts) — دالة جديدة تدفع حصة واحدة فقط بمعاملة (transaction)، تتحقق `payoutId IS NULL` (منع الدفع المزدوج)، تنشئ `profitSettlementPayouts` بمبلغ تلك الحصة وحدها، وتربط `payoutId` بهذا الصف فقط
- الزر المجمّع القديم (`settleMerchantPayout`) **بقي بدون أي تغيير**، الاثنين متاحان معاً بتبويب "أرصدة التجار"
- تحذير جديد بـ Dialog "تسوية الرصيد" المجمّع يوضّح إنه يفقّد ربط السكرينات، ويقترح استخدام الدفع الفردي بدلاً

### 3. رؤية الأسلاف لتفاصيل تابعيهم (أي عمق، بدون اشتراط امتلاك حصة)
- `isAncestorOf(ancestorId, merchantId)` — دالة جديدة تتحقق بالسيرفر (نفس منطق `findAllAncestorsWithOverride` لكن بدون فلترة `overridePercentage`) إن الطالب فعلاً سلف للتابع بأي عمق
- `getSubordinateShareDetailsForAncestor` — يرجع نفس مستوى التفصيل الموجود بـ`myShareDetails` (صافي الربح، كلفة الترويج، الصورة، تفاصيل كل طلب) لأي تابع، **بدون اشتراط أن يملك السلف حصة شخصية بتلك التسوية**
- `ancestorId` يُشتق دائماً من `ctx.merchant.id` (الجلسة الحقيقية) — لا يُقبل أبداً من `input` (حماية IDOR)
- قسم جديد بواجهة التاجر (MerchantTeam.tsx): "تفاصيل تسويات الفريق" — قائمة اختيار "اختر أي تابع لك" (تُغذّى من `myDescendants`، كل الأعماق)، وعند الاختيار تعرض تفاصيل تسوياته كاملة

### 4. قاعدة `canViewCosts` طُبّقت بصرامة على كل الميزات الجديدة
**قرار نهائي محسوم بهذي الجلسة (بعد نقاش مطوّل)**: إذا `canViewCosts=false` لتاجر (حتى لو كان سلف شرعي لتابع)، **لا يرى أي رقم مالي ولا صورة إطلاقاً** — لا صورة سكرين، لا صافي ربح، لا كلفة ترويج، لا سعر منتج، لا كلفة توصيل، لا مجموع عمولة. يرى فقط بيانات غير مالية (رقم الطلب، اسم المنتج، الكمية، التاريخ، اسم التاجر صاحب الطلب). هذا يطبّق بأسلوب "حذف الحقل بالكامل" (ليس تصفيره) عبر:
- `maskMerchantShareDetail` (لـ `myShareDetails`)
- `maskSubordinateSettlementDetail` (لـ `subordinateShareDetails`)
- `maskPayoutPromotionProof` (لـ `myPayouts`)
- بسبب هذا، كل هالـ endpoints ترجع نوع union (`X | MaskedX`)، والواجهة تتحقق بـ `'field' in data` قبل أي عرض — هذا يجبر TypeScript يمنع أي قراءة حقل مالي بدون تحقق أول (حماية إضافية على مستوى الترجمة، مو بس تشغيل)

### 5. تبويب "سجل الدفعات" (أدمن) — جديد بالكامل
- `getAllPayouts()` — كل الدفعات عبر كل التجار مجتمعة (LEFT JOIN احترازي مع merchants)، بدون فلترة، للأدمن فقط
- تبويب ثالث بـAdminProfitSettlements.tsx يعرض: التاجر، الرتبة، المبلغ، التاريخ، الملاحظة، صورة إثبات الدفع

### 6. ربط صورة إثبات الترويج بالدفعات الفردية غير الملتبسة
**مشكلة اكتشفها إبراهيم بنفسه أثناء الاختبار**: بعد دفع حصة (فردي أو مجمّع)، صورة إثبات الترويج الأصلية (المرفوعة وقت إنشاء التسوية) تختفي تماماً من كل مكان — لا تظهر لا للأدمن ولا للتاجر.

**الحل**: `resolvePayoutPromotionProofUrls` — دالة تربط صورة الترويج بدفعة فقط عندما تكون مرتبطة بحصة واحدة بالضبط (`profitSettlementShares.payoutId` بعدد صف = 1). دفعات `settleSingleShare` تحل دائماً (حصة واحدة دائماً)، دفعات `settleMerchantPayout` قد تحل أو لا حسب عدد الحصص الفعلي المجمّعة (وليس حسب نوع الدالة — إذا صادف أن دفعة مجمّعة كانت لحصة واحدة بالمصادفة، تُعرض أيضاً؛ إذا خلطت أكثر من حصة، تبقى فارغة "—" بدون تخمين). يتبع نفس قاعدة `canViewCosts` بواجهة التاجر.
- عمود جديد "صورة إثبات الترويج" بتبويب "سجل الدفعات" (أدمن) وبقسم "سجل الدفعاتي" بواجهة التاجر

## Commits هذي الجلسة (بالترتيب)
- `55ec449` — نظام دفع الحصص الفردية + عرض تفاصيل تسويات التابعين للأسلاف + سجل الدفعات الشامل للأدمن + تحذير التسوية المجمّعة + مكوّن ImageLightbox
- `d585332` — ربط صورة إثبات الترويج بسجل الدفعات الفردية غير الملتبسة + إخفاؤها حسب canViewCosts بواجهة التاجر

كلاهما مدفوعان بنجاح لـ `origin/main` (تم التحقق بـ`git fetch` + مقارنة مباشرة لـHEAD).

## مكوّن جديد قابل لإعادة الاستخدام
`client/src/components/ImageLightbox.tsx` — صورة مصغّرة قابلة للنقر تفتح Dialog بحجم كامل. يُستخدم الآن لكل صور الإثبات (ترويج/دفع) بكل من MerchantTeam.tsx وAdminProfitSettlements.tsx. لم يكن موجوداً أي مكوّن من هذا النوع بالمشروع قبل هذي الجلسة.

## اختبارات آلية
`pnpm run check` (tsc) و`pnpm test` (vitest، 56 اختبار) نظيفان بالكامل بعد كل تعديل بهذي الجلسة. لا يوجد اختبار مخصص للميزات الجديدة بالذات، لكن كل الاختبارات الموجودة (بما فيها منطق التسويات/العمولات القديم) ما زالت تعمل بدون كسر.

## نقاط مهمة للجلسة القادمة (لا شي معلّق تقنياً، لكن للتذكير)

### مهام مؤجلة من قبل (لم تُمس هذي الجلسة، لا تزال قائمة):
- **تقرير أرباح المنتجات** (شبكة منتجات بصور، ربح كل منتج بالفترة بلون أخضر/أحمر) — لم يبدأ
- **فلترة/بحث صفحة "إدارة الطلبات" (AdminOrders.tsx)** — مؤجلة
- **شركة التوصيل** — لا يوجد أي أثر لها بالكود الحالي، تحتاج تحقيق من جديد أو بناء من الصفر
- **فصل Manus بالكامل**: رفع الصور (`BUILT_IN_FORGE_API_URL`) واستدعاءات LLM (`server/_core/llm.ts`) لا تزال تشير لـ`forge.manus.im` — تحتاج جلسة مخصصة
- **فجوة تصميمية**: `updateMerchantByAdmin` لا يعيد التحقق من توافق التاجر مع أبنائه (children) عند تغيير الرتبة — يحتاج قرار تصميم متعمّد قبل التنفيذ

### ملاحظة إثرائية للجلسة القادمة (اقتراح، ليس قرار محسوم بعد):
أثناء هذي الجلسة، إبراهيم انتقد (بحق) أنني لم أستبق بعض الاحتياجات (تبويب سجل الدفعات، ربط الصورة بالدفعة) وانتظرت أن يكتشفها هو بنفسه أثناء الاختبار. القاعدة للجلسات القادمة: عند بناء أي ميزة مالية جديدة، فكّر استباقياً بسيناريوهات الاستخدام الكاملة (مو بس المسار السعيد) قبل عرض الخطة الأولى، بدل الاكتفاء بالرد على ما يُطلب حرفياً.

## مبادئ العمل الثابتة (لم تتغير، للتذكير)
1. المستخدم غير تقني، يرسل صور شاشة، يحتاج نصوص جاهزة للنسخ بالعربي
2. أي أمر "فحص/قراءة فقط" آمن ويوافَق عليه مباشرة؛ أي أمر "كتابة/تعديل/حذف/db:push" يحتاج مراجعة دقيقة
3. الأسرار لا تُكتب أبداً بمحادثة Claude.ai
4. عند أي شك بعرض diff معكوس/ملخبط بالطرفية (مشكلة RTL معروفة ومتكررة)، نطلب من Claude Code قراءة الملف الفعلي مباشرة (أداة Read) بدل الاعتماد على عرض الـ diff
5. الدقة المالية فوق كل شي: كل معادلة تُختبر برقم ملموس، كل تعديل كود يُراجع لحاله
6. **لا تشغّل `pnpm dev` من نافذة Claude Code أبداً** — يعلّق الطرفية. السيرفر يشتغل دائماً بنافذة PowerShell منفصلة تماماً
7. `db:push`/سكربتات تمس قاعدة البيانات الحية تحتاج موافقة صريحة منفصلة، ولا تُشغَّل تلقائياً حتى لو كانت "idempotent"
8. الاختبار الفعلي بالمتصفح **قبل** أي commit، مو بعده — خصوصاً لأي ميزة تمس المال أو الخصوصية

## تحديث لاحق: العمولة الثابتة تتضاعف مع الكمية

بعد جلسة 6، اكتُشف أن العمولة الثابتة (commissionType="fixed") كانت تُحسب مرة واحدة بغض النظر عن الكمية (quantity) بالطلب. تم تصحيحه:

- `computeFrozenCommission` (server/db.ts) — أضيف معامل رابع `quantity`، والنوع "fixed" الآن يُحسب `commissionValue × quantity` (النوع "percentage" لم يتغير، لأن `baseAmount` أصلاً يشمل الكمية).
- تحديث موقعي الاستدعاء: `physicalOrders.create` (routers.ts) يمرر `input.quantity` الفعلية، و`digitalSales.create` يمرر `1` ثابتة (توافقي فقط — المبيعات الرقمية لا تملك مفهوم كمية أصلاً، لا يغيّر أي سلوك فعلي).
- **لا يمس أي طلب قديم موجود بالفعل** — commissionAtOrderTime/commissionAtSaleTime تُكتب فقط لحظة إنشاء طلب جديد وتبقى مجمّدة، نفس مبدأ التجميد المعتمد بكل النظام.
- اختُبر فعلياً بالمتصفح: طلب بكمية 2 لتاجر عمولته الثابتة 1,000 أعطى عمولة 2,000 بالضبط.
- Commit: `fee2d7b` (server/db.ts + server/routers.ts فقط، مدفوع لـ origin/main).

## مهام لا تزال معلّقة من هذا التحديث (لم تُنفَّذ بعد):

1. **إصلاح عرض اسم المنتج**: بقودنص "الملخص" المنفصل بشاشة إدخال الطلب الفيزيائي (client/src/pages/MerchantPhysical.tsx:209-216)، لا يظهر اسم المنتج، فقط الرقم الإجمالي. الاسم يظهر فقط بمكان الاختيار (السطر 172). يحتاج إضافة اسم المنتج بقودنص الملخص أيضاً.

2. **حد أدنى اختياري لسعر المنتج**: ميزة جديدة مطلوبة — إضافة حقل "الحد الأدنى للسعر" اختياري لكل منتج فيزيائي (يحدده الأدمن بلوحة الإدارة). إذا لم يُحدد، السعر يبقى حراً بالكامل كالوضع الحالي (بدون تغيير). إذا حُدد، أي طلب بسعر أقل منه يُرفض برسالة خطأ واضحة. لا يوجد حد أعلى إطلاقاً. طُلب تقرير فحص لهذا (schema المنتجات، مكان التحقق من السعر عند إنشاء طلب، واجهة إضافة/تعديل منتج) لكن التقرير لم يصل/يُراجع بعد بهذي الجلسة.

3. **شركة التوصيل**: تأكد بحث شامل (git log --all, pickaxe search -S, grep على كل الفروع) أنها غير موجودة إطلاقاً بأي نقطة من تاريخ المشروع — تحتاج بناء كامل من الصفر إذا تقرر البدء بها. لم يُتخذ قرار بعد بخصوص متى نبدأ فيها.

## تحديث لاحق: حد أدنى اختياري لسعر المنتج (minPrice)

ميزة جديدة أُضيفت بعد تحديث "العمولة × الكمية": حد أدنى اختياري للسعر لكل منتج فيزيائي، يحدده الأدمن.

- `drizzle/schema.ts`: عمود جديد `minPrice` (nullable) بجدول `physicalProducts`. لا قيمة = بدون حد أدنى، السعر حر بالكامل (سلوك افتراضي، لا يمس أي منتج قديم). لا يوجد حد أعلى إطلاقاً.
- `server/db.ts`: توقيعا `createPhysicalProduct`/`updatePhysicalProduct` يقبلان `minPrice?: number` — بدون تعديل بجسم الدالتين (data تُمرَّر كما هي).
- `server/routers.ts`: Zod schema لـ `physicalProducts.create`/`update` يقبل `minPrice`. التحقق الفعلي بـ`physicalOrders.create` — إذا `product.minPrice` محدد و`input.productPrice` أقل منه، يُرفض الطلب بـ`TRPCError` (BAD_REQUEST) برسالة عربية واضحة. يُطبَّق فقط عند وجود `productId` مرتبط فعلياً (اختيار من الكتالوج) — الإدخال اليدوي الحر يبقى بدون أي قيد.
- `client/src/pages/AdminProducts.tsx` (PhysicalProductDialog): حقل جديد "الحد الأدنى للسعر (د.ع) - اختياري" بنفس فورم إضافة/تعديل منتج، بجانب حقلي wholesaleCost/deliveryCost الموجودين.
- Migration: `drizzle/0012_curious_katie_power.sql` — `ALTER TABLE physical_products ADD minPrice int` فقط (عمود جديد nullable، لا تعديل/حذف على أي جدول آخر).
- اختُبر فعلياً بالمتصفح: تحديد حد أدنى لمنتج، محاولة طلب بسعر أقل → رُفض برسالة واضحة؛ طلب بسعر مساوٍ أو أعلى → قُبل عادي.
- Commit: `4155abf` (schema.ts + migration + meta + db.ts + routers.ts + AdminProducts.tsx، مدفوع لـ origin/main).

## مهام لا تزال معلّقة (لم تُنفَّذ بعد، من طلبات سابقة بنفس الجلسة):

1. **تكبير صورة المنتج بنافذة "اختر منتجاً من الكتالوج"**: الصورة حالياً صغيرة وغير واضحة بشاشة إدخال الطلب الفيزيائي، التاجر يريد الضغط عليها لعرضها بحجم أكبر (مكوّن ImageLightbox الموجود من قبل بالمشروع صالح لهذا الغرض مباشرة).

2. **إظهار اسم المنتج بقودنص "الملخص"**: بشاشة إدخال الطلب الفيزيائي (client/src/pages/MerchantPhysical.tsx:209-216)، قودنص الملخص المنفصل يعرض السعر الإجمالي فقط بدون اسم المنتج. الاسم يظهر فقط بمكان الاختيار (السطر 172).

3. **تقرير أرباح المنتجات**: الميزة الأصلية المطلوبة (شبكة منتجات بصور، ربح كل منتج بالفترة بلون أخضر/أحمر، تفاصيل عند الضغط) — لم يبدأ العمل عليها بعد.

4. **شركة التوصيل**: لا تزال غير موجودة إطلاقاً بأي نقطة من تاريخ المشروع (تأكد ببحث شامل). تحتاج بناء كامل من الصفر إذا تقرر البدء بها. لم يُتخذ قرار بعد بخصوص التوقيت.
